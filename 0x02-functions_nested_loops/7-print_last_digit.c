#include "main.h"

/**
 * print_last_digit - Prints the last digit
 * of  number
 *
 * Return last digit
 */
int print_last_digit(int)
{
	int i;

	if (i < 0)
		{
		_putchar(-1 + 48);
	return (-1);
		}
	else
	{
	_putchar(1 + 48);
	return (1);
	}
}
